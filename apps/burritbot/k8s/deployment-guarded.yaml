# ABOUTME: Guarded BurritBot Deployment — traffic enters via the burritbot-net gateway.
# ABOUTME: Carries the full burritbot.io label set enforced by require-burritbot-labels.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: burritbot
  namespace: burritbot-guarded
  labels:
    app.kubernetes.io/name: burritbot
    app.kubernetes.io/part-of: burritbot
    burritbot.io/layer: application
    burritbot.io/guarded: "true"
    burritbot.io/model-source: "vertex-ai/gemini-3-pro"
    burritbot.io/model-hash: "sha256:deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: burritbot
      burritbot.io/guarded: "true"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: burritbot
        app.kubernetes.io/part-of: burritbot
        burritbot.io/layer: application
        burritbot.io/guarded: "true"
        burritbot.io/model-source: "vertex-ai/gemini-3-pro"
        burritbot.io/model-hash: "sha256:deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
    spec:
      serviceAccountName: burritbot
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: burritbot
          image: us-west1-docker.pkg.dev/REPLACE_WITH_PROJECT/burritbot/burritbot:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: MODEL_NAME
              value: gemini-3-pro
            - name: GOOGLE_CLOUD_PROJECT
              value: REPLACE_WITH_PROJECT
            - name: GOOGLE_CLOUD_LOCATION
              value: us-west1
            - name: BURRITBOT_GUARDED
              value: "true"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.monitoring.svc.cluster.local:4317
            - name: OTEL_SERVICE_NAME
              value: burritbot-guarded
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
