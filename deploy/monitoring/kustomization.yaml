# ABOUTME: ArgoCD sync target for The Eyes — kube-prometheus-stack, OTel Collector, dashboards.
# ABOUTME: Stub: deployable manifests for these components do not yet exist in the repo.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []

# TODO(critical-fixes-plan): observability/ ships config files only — no
# Deployment for the OTel Collector, no kube-prometheus-stack helm release,
# no Grafana dashboard ConfigMaps. Each needs its own manifest before it
# can be referenced here. Tracked as a follow-up — out of scope for the
# critical-fixes pass.
